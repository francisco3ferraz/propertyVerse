<!DOCTYPE html>
<html>
  <head>
    <title>PropertyVerse - New Experiences, Same Comfort</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= favicon_link_tag asset_path('logo.svg') %>
    <%= javascript_importmap_tags %>

    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/vanillajs-datepicker@1.3.1/dist/css/datepicker.min.css">

    <% if controller_name == "reservations" && action_name == "new" %>
      <%= stripe_javascript_tag %>
    <% end %>

  </head>
  

  <body>
      <%= render "layouts/header"%>
      <%= yield %>
  </body>
</html>
